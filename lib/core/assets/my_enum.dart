enum TypeCrud { CREATE, UPDATE, DELETE }

enum Flavor { DEV, STAGING, PRODUCTION }

enum Status { SUCCESS, ERROR }

enum TextStyleWeight {
  Title,
  Title2,
  Title3,
  subtitle1,
  subtitle2,
  subtitle3,
  body,
}

enum TypeSnacbar { ERROR, INFO, WARNING }

enum TypeMap {MAP_PICKER, MAP_OPEN, MAP_CHECK_IN}
