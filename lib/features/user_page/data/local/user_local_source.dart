class UserLocalSource {
  // final HCSSODao _hrisDao;
  // SettingService _settingService;

  // UserLocalSource(this._hrisDao);

  // Future<usr.UserData> getDataUser() async {
  //   _settingService = await SettingService.getService();
  //   var data = await _settingService.get(MyCons.myUser);
  //   return usr.UserData.fromJson(json.decode(data));
  // }
}
